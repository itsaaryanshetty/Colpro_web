version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:80"       # Access app at http://localhost:5173
    restart: unless-stopped